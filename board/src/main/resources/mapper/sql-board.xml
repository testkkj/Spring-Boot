<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.ord//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.board.mapper.BoardMapper">  <!-- 매퍼와 xml을 메칭 시키는데 필요한 경로 board.board.mapper.BoardMapper.selectBoardList 중 파일이 위치한 경로를 공통으로 사용 id에서 해당 메소드를 지정한다 -->
    <select id="selectBoardList" resultType="com.example.board.dto.BoardDto"> <!-- select 쿼리임을 나타내고 id는 매퍼의 메서드 명과 같다 -->
        <![CDATA[
            SELECT
                board_idx,
                title,
                hit_cnt,
                DATE_FORMAT(created_datetime, '%Y.%m.%d %H:%i:%s') AS created_datetime
            FROM
                t_board
            WHERE
                deleted_yn = 'N'
                ORDER BY board_idx DESC
        ]]>
    </select>
</mapper>